{
  if (SHOW_LOGS)   Logger.v(TAG,"startPlayback");
  mPlayerHandler.addMessages(Arrays.asList(new CreateNewPlayerInstance(videoPlayer,this),new SetUrlDataSourceMessage(videoPlayer,videoUrl,this),new Prepare(videoPlayer,this),new Start(videoPlayer,this)));
}
