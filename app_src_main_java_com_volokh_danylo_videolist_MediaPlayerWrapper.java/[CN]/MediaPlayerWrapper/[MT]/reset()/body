{
  if (SHOW_LOGS)   Logger.v(TAG,">> reset , mState " + mState);
synchronized (mState) {
    mMediaPlayer.reset();
    mState.set(State.IDLE);
  }
  if (SHOW_LOGS)   Logger.v(TAG,"<< reset , mState " + mState);
}
