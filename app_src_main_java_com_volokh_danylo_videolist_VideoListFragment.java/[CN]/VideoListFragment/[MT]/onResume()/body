{
  super.onResume();
  if (!mList.isEmpty()) {
    mListView.post(new Runnable(){
      @Override public void run(){
        mVideoVisibilityCalculator.onScrollStateIdle(VideoListFragment.this,mListView.getFirstVisiblePosition(),mListView.getLastVisiblePosition());
      }
    }
);
  }
}
